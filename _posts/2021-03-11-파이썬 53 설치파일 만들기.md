참고: https://blog.naver.com/PostView.nhn?blogId=qbxlvnf11&logNo=221791248065





pycharm의 terminal에서 pyinstaller를 설치

```python
pip install pyinstaller
```



pycharm의 터미널에서

test210312a의 경우 main.py가 실행용 소스 파일이다.

```pyrhon
pyinstaller main.py					// (1)
```



dist폴더에 main.exe파일이 생성된다.





### 실행 파일 이름 변경

참고: https://developer-mistive.tistory.com/59

(1)에서 실행하고 나면, main.spec파일이 생성된다. 

(2)에서 실행파일 명을 변경한후,아래와 같이 실행하면, st1.exe파일이 생성된다.

```
pyinstaller main.spec
```



```
# -*- mode: python ; coding: utf-8 -*-

block_cipher = None


a = Analysis(['main.py'],
             pathex=['E:\\_ygkim\\_Project\\python\\test210312a'],
             binaries=[],
             datas=[],
             hiddenimports=[],
             hookspath=[],
             runtime_hooks=[],
             excludes=[],
             win_no_prefer_redirects=False,
             win_private_assemblies=False,
             cipher=block_cipher,
             noarchive=False)
pyz = PYZ(a.pure, a.zipped_data,
             cipher=block_cipher)
exe = EXE(pyz,
          a.scripts,
          [],
          exclude_binaries=True,
          name='st1',													// (2)
          debug=False,
          bootloader_ignore_signals=False,
          strip=False,
          upx=True,
          console=True )
coll = COLLECT(exe,
               a.binaries,
               a.zipfiles,
               a.datas,
               strip=False,
               upx=True,
               upx_exclude=[],
               name='main')

```

