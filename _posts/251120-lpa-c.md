# lpa_bist



### State macros to enum

old:

```
`define WBITS	180
`define IDLE_STATE  0
`define WRITE_STATE 1
`define WRITE_LPA 	2
`define READ_LPA  	3
`define SHIFT_LPA  	4
//`define SLD 10
```



new:

```
  typedef enum logic [2:0] {
    ST_IDLE      = 3'd0,  // `IDLE_STATE
    ST_WRITE     = 3'd1,  // `WRITE_STATE
    ST_WRITE_LPA = 3'd2,  // `WRITE_STATE_LPA
    ST_READ      = 3'd3,  // `READ_LPA
    ST_SHIFT     = 3'd4   // `SHIFT_LPA
  } state_t;
```



### Bist only macros -> localparam or parameters in  lpa_cfg_pkg

old

```
//read command defines
`define COLSIZE 1440
//`define COLSIZE 140
`define B_CNT_BASE 25
`define B_LOADER_STR 1
`define B_TAKEOVER_STR `B_CNT_BASE
`define B_LEAKER_STR 1
`define B_BSENSE_STR `B_CNT_BASE * 2
`define B_BSENSE_END `B_BSENSE_STR+ 127
`define B_RPCHG_STR  `B_BSENSE_END 
`define B_RPCHG_END  `B_RPCHG_STR + `B_CNT_BASE
`define B_BSENSE_STR1  `B_RPCHG_END + (`B_CNT_BASE * 2)
`define B_BSENSE_END1  `B_BSENSE_STR1 + `B_CNT_BASE
//`define B_RCMD_STR	   `B_BSENSE_END1 + (`B_CNT_BASE * 2)
`define B_RCMD_STR	   `B_CNT_BASE
`define B_RCMD_END	   `B_BSENSE_END1 + (`B_CNT_BASE * 2) + `B_CNT_BASE
//`define B_RCMD_END 	   `B_RCMD_STR + `B_CNT_BASE
//`define B_SCLK_STR	   `B_RCMD_STR
`define B_SCLK_STR	   `B_BSENSE_END1 + (`B_CNT_BASE * 2)
`define B_SCLK_END 	   `B_RCMD_END

`define B_ROWP_STR   `B_RPCHG_END  + `B_CNT_BASE
`define B_ROWP_END	 `B_BSENSE_END1 + `B_CNT_BASE
//`define B_READ_END   `B_RCMD_END + 127
`define B_READ_END   `B_RCMD_END + `B_CNT_BASE + `COLSIZE/4

`define B_READ_SHIFT_STR   `B_RCMD_END + `B_CNT_BASE	 
```

new

```
  localparam int B_CNT_BASE      = 25;
  localparam int B_LOADER_STR    = 1;
  localparam int B_TAKEOVER_STR  = B_CNT_BASE;
  localparam int B_LEAKER_STR    = 1;

  localparam int B_BSENSE_STR    = B_CNT_BASE * 2;
  localparam int B_BSENSE_END    = B_BSENSE_STR + 127;

  localparam int B_RPCHG_STR     = B_BSENSE_END;
  localparam int B_RPCHG_END     = B_RPCHG_STR + B_CNT_BASE;

  localparam int B_BSENSE_STR1   = B_RPCHG_END + (B_CNT_BASE * 2);
  localparam int B_BSENSE_END1   = B_BSENSE_STR1 + B_CNT_BASE;

  localparam int B_RCMD_STR      = B_CNT_BASE;
  localparam int B_RCMD_END      = B_BSENSE_END1 + (B_CNT_BASE * 2) + B_CNT_BASE;

  localparam int B_SCLK_STR      = B_BSENSE_END1 + (B_CNT_BASE * 2);
  localparam int B_SCLK_END      = B_RCMD_END;

  localparam int B_ROWP_STR      = B_RPCHG_END + B_CNT_BASE;
  localparam int B_ROWP_END      = B_BSENSE_END1 + B_CNT_BASE;

  localparam int B_READ_END      = B_RCMD_END + B_CNT_BASE + COLSIZE/4;
  localparam int B_READ_SHIFT_STR = B_RCMD_END + B_CNT_BASE;
```





# lpa_core



# lpa_ctrl_blk



### Default value for RJ1_REG macros -> parameters in lpa_cfg_pkg

```
`define ROWP_STR 2
`define ROWP_DLY_STR 6     //delta between rowp_dly and rowp is the write_n pulse width
`define BLD_STR 0
`define BLD_END 7 
`define DATA_LATCH_STR 0
`define COL_UPD_STR 0
`define COL_UPD_END 3 
`define END_OF_CNT 10
```

[Q] HLA_?? 어떻게? -> 기존 코드들 확인해보자 -> pkg에 있나? 

: 이값들이 RJ1_REG에 필요한가?

```
parameter  wba=`HLA_WBA, wbw=`HLA_WBW, wbs=`HLA_WBS,wbr=8
```





